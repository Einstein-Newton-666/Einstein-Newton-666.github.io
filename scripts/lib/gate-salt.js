'use strict';

/* ---------------------------------------------------------------------------
 * 访问密码的固定 salt。
 *
 * 不是秘密，随仓库公开（salt 的作用只是让不同站点/不同密码之间不能共用预计算表）。
 * 固定下来才能让访客浏览器里"记住 30 天"的解锁钥匙在每次重新部署后继续有效；
 * 换掉它就等于作废所有已解锁的浏览器（换密码也有同样效果）。
 *
 * 放在 scripts/lib/ 而不是 scripts/ 根目录：Hexo 会把 scripts/ 下的每个文件当脚本
 * 加载，一个 .txt 会被当成 JS 解析并报 SyntaxError。
 * ------------------------------------------------------------------------- */

module.exports = 'G0UZ6MX6vIbWnpeyNLzt6Q==';
